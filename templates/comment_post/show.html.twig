{% extends 'base.html.twig' %}

{% block title %}CommentPost{% endblock %}

{% block body %}
    <h1>CommentPost</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ comment_post.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ comment_post.Title }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ comment_post.Content }}</td>
            </tr>
            <tr>
                <th>CreateAt</th>
                <td>{{ comment_post.CreateAt ? comment_post.CreateAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Author</th>
                <td>{{ comment_post.Author }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('comment_post_index') }}">back to list</a>

    <a href="{{ path('comment_post_edit', {'id': comment_post.id}) }}">edit</a>

    {{ include('comment_post/_delete_form.html.twig') }}
{% endblock %}
